<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="styles/styles.css">
    <title>Day Tasks</title>
</head>

<body class="bg-body-tertiary bg-image">
    <nav class="tasksNavbar bg-secondary">
        <a href="/" class="navLink text-body-secondary">Home</a>
        <a href="/workTasks" class="navLink text-body-secondary">Work Tasks</a>
    </nav>

    <div class="container">
        <h1 class="taskListHeading text-body-secondary"><em> Day's To-Do List </em></h1>
        <form action="/add" method="POST">
            <input class="taskEnterInput" type="text" name="task" placeholder="Add a new task..." required>
            <button class="btn btn-secondary tasksAddButton" type="submit">Add +</button>
        </form>
        <ul>
            <% tasks.forEach((task, index)=> { %>
                <div class="tasksRow">
                    <li class="tasksLi">
                        <input type="checkbox" name="completed" value="<%= index %>">
                        <label for="task<%= index %>" class="strikethrough">
                            <%= task.text %>
                        </label>
                    </li>
                </div>
                <% }); %>
        </ul>
    </div>

    <footer>
        © 2023 Gaurav Sharma . All rights reserved.
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>